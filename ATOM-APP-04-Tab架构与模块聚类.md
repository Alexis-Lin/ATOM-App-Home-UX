# ATOM App · Tab 架构与模块聚类

> 版本：v0.2（讨论稿）　|　日期：2026-08-04　|　受众：产品 / 设计 / 研发
> 缘起：2026-08-04 讨论——九大模块(Home / Program / AI Agent / Exercises / History·Trend / Achievements / Challenges / Community / Profile)如何聚类进底部导航。本文承接 `ATOM-APP-03`(计划 Tab)并扩展为全 Tab 架构;决策项挂总纲 D19,并修订 D15。
> v0.2 变更:采纳「**分组 Tab + 独立 AI 入口**」布局 `{[tab×4][AI]}`(替代 v0.1 五槽中央按钮);社区放缓,首发四 Tab=首页/训练/成长/我的,并预写社区上线时的既定迁移路径。

---

## 1. 推荐方案(首发态)

```
┌──────────────────────────────────────┬─────────┐
│  首页    训练    成长    我的           │  ◎ ATOM │
│  Home   Train  Progress  Me          │  AI 入口 │
└──────────────────────────────────────┴─────────┘
     ↑ Tab 组(地方,可停留)                ↑ 分离按钮(能力,随时召唤)
```

**布局语义**:视觉分组即信息架构——四个 Tab 是「地方」,AI 按钮是「能力」;分离式布局把这个区别直接画在底栏上,比中央 FAB(形似第五个 Tab)更诚实。系统级支持:iOS 26 分离式底栏 / tab bar accessory、Material 3 navigation bar + FAB,原生组件可实现。

| 槽位 | 聚类模块 | 回答的问题 | 数据闭环角色(P1) |
|---|---|---|---|
| 1 首页 | Home 三区模型 + 人格问候 | 现在做什么 | PLAN 今日执行面 |
| 2 训练 | **Program** + **Exercises** + 课程探索(D17 v0.2:计划主页 + 动作库全屏二级页 + 探索内联) | 我在练的体系是什么、怎么调 | GOAL→PLAN 方向盘 |
| 3 成长 | **History/Trend** + **Achievements**(数据趋势 + 成就墙) | 我练成什么样了 | LOG/STATUS 里程表 |
| 4 我的 | **Profile**(档案 + 设置) | 我是谁 | IDENTITY/SETTINGS |
| ◎ AI | **AI Agent** + ⚡快速开练(收编 D15) | 我想问/说、立刻动 | 对话入口,横跨全环 |
| (无 Tab) | **Challenges**:首页 C 区活动卡 + 铃铛活动中心(D11,v1 三层运营结构) | — | 运营 |
| (延后) | **Community**:上线时按 §3 既定路径迁移 | — | 外部动机 |

九模块全覆盖:七个有即时归宿,Challenges 走运营位,Community 延后但路径已定。

## 2. 聚类判断

### 2.1 AI Agent = 分离式全局入口,不做常规 Tab
- Tab 是「去停留的地方」,Agent 是「随时召唤的能力」;对话做成 Tab 易成空落地页,且制造「第二个首页」与 Home 抢「我今天该干嘛」的心智。
- 分离按钮的收益:①全局一步可达(所有 Tab 下常驻);②与设备端 Action Button/「Hi ATOM」**跨端同构**(R2/R10——同一个管家的肌肉记忆),App 端长按可直接进语音输入;③收编 ⚡快速开练:点按开 Agent 面板,首排即「⚡快速开练」动作芯片,非对话路径不受损;④分组视觉明示「能力≠地方」。
- 竞品佐证:Keep 卡卡=入口非 Tab;Strava 把最高频动作(Record)独立处理。

### 2.2 Exercises、Challenges 不独占 Tab
- 动作库=字典 → 训练 Tab 第三段(D17 已论证),主触达仍是课程内情境跳转。
- Challenges=运营化社交(与他人在同一目标下比) → 归社区 Tab;首页 C 区保留一张活动卡作入口(三层运营结构不变)。

### 2.3 Achievements + History/Trend 合并为「成长」Tab
- 两者是「我练成什么样了」的两种口吻(数据/荣誉),合并即完整「里程表」陈列面。
- 成就高光不靠 Tab 曝光:达成瞬间在首页 B 区回流卡庆祝、(未来)一键分享到社区——**在别处发生,在成长陈列**。
- 社区放缓使「成长」得以独立成 Tab;社区上线后按 §3 并入「我的」(Strava 2025 You Tab 同款终态)。

### 2.4 Challenges 首发不占 Tab
无社区 Tab 阶段,挑战走 v1 已定的三层运营结构:首页 C 区活动卡位(展示)+ 铃铛活动中心(全集,D11)。为单一运营品类开 Tab 会太薄。

## 3. 社区上线时的既定迁移路径(现在写死,避免摇摆)

```
首发:  [首页  训练  成长  我的] [◎AI]
未来:  [首页  训练  社区  我的] [◎AI]     ← 只动一个槽位
        成长 → 并入「我的」(数据+成就墙下沉一级)
        Challenges → 迁入社区 Tab(首页 C 区卡位保留)
```

一次到位、只动第 3 槽;「我的」终态=Profile+成长(Strava You 同款)。Keep 反复摇摆的教训=迁移可以有,但必须是**预先声明的一次性动作**。→ 挂 D19。

## 4. 护栏(写进契约)

1. **Agent 不复刻模块**:可深链到任何页面(「帮我调计划」→训练 Tab 对应页),不在对话内重建计划管理/数据面板——否则五 Tab 被逐渐掏空。
2. **每 Tab 一个问题**:任何新模块入 Tab 前先回答归属哪个问题;答不出=不上 Tab(先做入口/卡位)。
3. Home 三区契约、社区 feed 不上首页(D13)、执行/管理分界(03 §3)继续有效。

## 5. 决策项

| # | 议题 | 状态 |
|---|---|---|
| D19 | 底部导航定稿:`{[首页 训练 成长 我的][◎AI]}` 分离式布局 + §3 迁移路径 | 🟡 方向已合意(2026-08-04),细节待交互稿 |
| D15(修订) | ⚡快速开练并入 AI 入口:点按=Agent 面板(首排快练芯片)、长按=语音输入(候选)——交互稿后可用性验证 | ⏳ 待定稿 |

## 6. 对现有文档的影响

- `ATOM-APP-03` v0.2:计划主页+动作库全屏二级页(无顶部分段),Tab 定名「训练」倾向确认(D17)。
- `ATOM-APP-02` wireframe:tabbar 已更新为分组四 Tab + 独立 ◎ATOM 按钮。
- `ATOM-APP-01` §4 CTA 矩阵:⚡快速开练动线描述随 D15 修订。
