# ATOM App Home · 信息架构与状态框架

> 版本：v0.1（讨论稿）　|　日期：2026-08-04　|　受众：产品 / 设计 / 研发
> 性质：把 `ATOM-APP-01` 的三区立意落成**可画图、可开发**的框架:模块落位 + 状态体系 + 关键快照。配套低保真线框 [`wireframe/home-wireframe-v0.html`](wireframe/home-wireframe-v0.html)(浏览器打开,顶部切换 7 个状态快照)。

---

## 1. 三区骨架 × 模块落位

v1 模块清单按三区契约重新归位:

| 区 | 模块 | 来源 | 备注 |
|---|---|---|---|
| 顶部 | 人格问候一句话 + 铃铛(红点) | v1 问候升级(D2/R4/P3) | Natter 驱动,消费全部状态维度,只输出一句话 |
| 顶部 | 设备条(压扁/警示/隐藏) | v1 规则继承 | 无设备用户隐藏(D4) |
| 顶部 | 强时效公告条 | v1 继承 | 稀缺性原则不变 |
| **A 今日执行** | 今日主卡(状态驱动,见 §3) + 一句「为什么」(D14) + 主 CTA | v1 动态主区升级 | **排他区:唯一主角,永在首屏** |
| **A** | 新手任务清单(仅 L0) | v1 继承 | 完成即消失 |
| **B 与我相关** | 主目标进度模块(P4 四选一,D16) | 🆕 | 随主目标切换 |
| **B** | 周历 + Streak(统一口径 R6/D10) | v1 周历+连胜合并 | |
| **B** | 回流亮点卡(仅训练后时段:质量评分/能量签 R5) | 🆕 | 自动过期 |
| **B** | 本周数据条(次/时长/千卡) | v1 继承,降为 B 区次要 | 长期可能被目标口径进度取代 |
| **C 探索运营** | 动作库 H1 区块(标题行+部位快捷入口→全屏库) | 🆕(2026-08-04) | 关键工具,首页与训练 Tab 双 H1 曝光;本体在全屏二级页 |
| **C 探索运营** | 官方活动轮播卡位 | v1 继承 | 三层运营结构不变 |
| **C** | 精选训练/计划(按 PREFERENCE 过滤,P6) | v1 继承+个性化 | 从「人找内容」到「内容适配人」 |
| **C** | 训练答疑 Tips(仅 L0/L1 早期) | v1 继承 | |
| **C** | 模板入口(空态折叠) | v1 继承 | |
| **C** | (未来)社区 digest 卡位 | D13 预留 | feed 不上首页 |
| 底部 | Tabbar + ⚡快速开练 FAB(D15) | v1 FAB 语义确认 | 全局兜底,一步开练 |

## 2. 状态梳理方法:五维正交 + 分区状态机

**先拆维度,再按区收敛,不做页面级组合矩阵**(五个维度直接相乘会得到数百种组合,不可设计也不可测试)。

### 2.1 五个正交状态维度

| 维 | 取值 | 主要影响 |
|---|---|---|
| **L 生命周期** | L0 新用户未首训 / L1 计划中老用户 / L2 无计划老用户 / L3 流失回访(≥14 天未开) | A 区主卡分支、新手任务显隐 |
| **P 计划日状态**(仅 L1) | 今日有课 / 今日休息日 / 今日已完成 / 计划到期待续 | A 区主卡内容 |
| **D 设备** | 已连接 / 断连·异常 / 无设备(是否服务待 D4) | 设备条形态、A 区是否被警示接管 |
| **T 时段情境** | 常规 / 训练后数小时 / 晚间未练 | 问候口味、B 区回流卡、(晚间)推送而非首页 |
| **M 会员** | Free / Pro | C 区内容与门控标识,不改骨架 |

### 2.2 分区状态机(呼应 Companion R3 仲裁思想)

页面不持有全局状态;**每个区各自消费相关维度**:

- **A 区 = 单一 slot + 决策树**(见 §3):同一时刻只渲染一个主卡,优先级仲裁,与 Companion「内容叠加层只出一个主角」同构。
- **B 区 = 模块开关矩阵**:各模块独立判断显隐(进度模块需「有目标+有 STATUS 数据」;周历需「有训练历史」;回流卡需「T=训练后」),互不抢位,缺席即塌缩。
- **C 区 = 内容槽 + 过滤器**:结构与状态基本无关,只按 PREFERENCE/M 过滤内容和排序。
- **顶部问候 = 人格层**:读所有维度,永远只输出一句话,永不长高。

### 2.3 A 区主卡决策树(草案)

```
1. D=断连·异常 且 今日需要设备训练?
   └ 是 → 设备警示卡(重新连接 / 排查)          [阻断型才接管 A 区]
2. L0 新用户 → 首训英雄卡(开始首训 10min) + 新手任务清单
3. L3 流失回访 → 欢迎回来卡(轻量重启:5-10min 找回状态课,不指责缺勤)
4. L2 无计划 → 生成计划英雄卡(立即生成 / ⚡快速开练)
5. L1 计划中:
   ├ 今日已完成 → 完成态卡(今日成果摘要 + 可选加练/拉伸)
   ├ 今日休息日 → 恢复日卡(人格表达 + 开始拉伸,D8)
   ├ 计划到期  → 续期卡(下一周期建议 / 重新生成)
   └ 今日有课  → 今日训练卡 + 为什么(D14) + 开始训练   [默认主态]
```

> 非阻断型设备异常(如低电量)不接管 A 区,走设备条警示态或 S2 式通知,避免「为了充电耽误开练」。

### 2.4 关键快照(设计 / 评审 / 验收的公共语言)

从组合空间中取**高频 + 高风险**的 8 个快照命名,后续视觉稿、开发验收、数据看板都用同一套编号:

| 快照 | 组合 | 一句话验收 |
|---|---|---|
| HS-1 新用户首开 | L0 · D连接 | 10 分钟内完成首训的路径无歧义 |
| HS-2 日常·今日有课 | L1·有课 · D连接 · T常规 | 打开→开始训练 ≤2 次点击 |
| HS-3 休息日 | L1·休息 | 不空白、不焦虑,有恢复动线 |
| HS-4 训练后回流 | L1·已完成 · T训练后 | 成果被看见(评分/能量签/进度前移) |
| HS-5 无计划老用户 | L2 | 生成计划为主,快速开练兜底 |
| HS-6 设备断连·有课 | L1·有课 · D断连 | 修复路径清晰,修复后直落训练 |
| HS-7 流失回访 | L3 | 无负罪感,重启成本极低 |
| HS-8 无设备(待 D4) | D无设备 | 跟练动线成立(若服务) |

## 3. 与 wireframe 的对应

`wireframe/home-wireframe-v0.html` 实现 HS-1 ~ HS-7(HS-8 待 D4 拍板):灰阶低保真,每个模块标注数据来源(PLAN/GOAL/LOG/PREFERENCE/运营位),三区用左侧色带标识。用途:评审三区骨架与状态切换逻辑,**不代表视觉方案**(视觉语言归 D6)。

## 4. 待确认

- [ ] A 区决策树优先级(尤其 L3 流失回访是否高于 L2 判定)
- [ ] 「今日已完成」态是否保留加练入口(过度训练风险 vs 意犹未尽)
- [ ] 本周数据条去留(与主目标进度模块的关系)
- [ ] HS-8 无设备态(随 D4)
