# ATOM App Home · Companion 呼应点参考

> 版本：v1.0　|　日期：2026-08-04　|　受众：产品 / 设计 / 研发
> 性质：**参考基线**。将设备端 [`ATOM-Companion-Home`](https://github.com/Alexis-Lin/ATOM-Companion-Home) 已定的逻辑提炼成可供 App 首页讨论直接引用的条目（R#）。**本文只摘录不改写**;设备端为准，源头见各条目「源」。

---

## R1 · 产品定位：拟人化 AI 管家

ATOM 不是「一个 App」，而是**拟人化 AI 管家**：设备端开机默认是一张有人格的表情卡，是整个系统的「脸」和情绪出口;功能交给平行卡片生态（连胜、每日一练、Workout、日历、Timer、喝水…）。
**对 App 的含义**：App 首页若呼应人格，需回答「同一个管家在手机上如何现身」——是第二张脸，还是管家的仪表盘。
源：`ATOM-00-总纲` §1、§4。

## R2 · 三态模型：按「谁发起」分层

设备端首页按发起方分三态，代码解耦、可分期：

| 态 | 发起方 | 内容 |
|---|---|---|
| S1 自主表达 | ATOM 自己 | S1a 默认表情 + S1b 碎碎念/能量签（情境引擎驱动） |
| S2 通知 | 其他应用/系统 | Workout / 连胜 / 喝水 / Timer / 低电量… |
| S3 语音 | 用户 | 唤醒→倾听→识别→思考→应答→结果 |

**对 App 的含义**：「谁发起」是一个可移植的信息架构切分法——App 首页同样存在「系统推给你的 / 你主动要的 / 产品人格自发说的」三类内容。
源：`ATOM-00-总纲` §2、`ATOM-01-首页状态机` §2。

## R3 · 注意力仲裁与优先级

`P0 紧急 > P1 通知 > P2 人格 > P3 待机`;同一时刻内容叠加层**只出一个主角**;免打扰只累积角标。
**对 App 的含义**：首屏若有多个模块争夺注意力（弹窗/横幅/角标/人格话术），可复用同一套优先级词汇与仲裁思路。
源：`ATOM-00-总纲` §7、`ATOM-01` §4。

## R4 · 碎碎念 Natter（人格引擎）

意图 × 变体，按情境（时段/天气/streak/久置/触摸/节日/生日）选择;60s 随机间隔、10s 自动消失;离/在线混合内容源;近 14 天去重;高频不发声（静默）。
**对 App 的含义**：App 端若做问候语/人格话术，可直接复用意图×变体模型与内容池（见 R8 数据契约），呈现形态另定。
源：`ATOM-02-S1-PRD` 附录A、`ATOM-00` §8-9。

## R5 · 能量签 Energy Draw（激励机制）

完成一组 Set 解锁一批 5 条，单日上限 3 批;命中 1/6·1/3·1/2、5 次保底;稀有度 普通/稀有/SSR = 70/25/5;主题×语言×Pro 门控（原创免费，名人名言/古诗/名著 = Pro）;声音只留给稀有时刻（揭晓音是唯一常规音效）。
**对 App 的含义**：这是「训练行为→情绪奖励」的既定闭环。App 首页可作为能量签的第二展示/收藏面，或复用其解锁信号做首页激励——需在 D2 讨论。
源：`ATOM-02-S1-PRD` 附录B、`ATOM-00` §8-9。

## R6 · 连胜 Streak

信号定义：**当日 ≥1 组 Set 即续连胜**;设备端为独立连胜卡，提醒走 S2 通知，配色连胜橙 `#ff9f43`。
**对 App 的含义**：App 首页大概率展示同一 Streak——应共用同一信号源与判定,避免两端数字不一致（对应总纲 D3）。
源：`ATOM-00-总纲` §8。

## R7 · 设计语言（设备端）

暗底（`#06/#14`）+ 高对比、文字纯白;强调蓝 `#3a6df0`、连胜橙 `#ff9f43`;通知按类型着色（训练绿/电量红/系统蓝）;动效曲线 `cubic-bezier(.2,.75,.2,1)`;emoji 克制（0–1）;两档文字气泡（碎碎念=无框自动消失,能量签=引号「」常驻到点掉）。
**对 App 的含义**：若 App 首页要与设备「一家人」，可继承配色 token 与动效曲线;暗底是否照搬待 D6 讨论（手机使用场景与圆屏桌面不同）。
源：`ATOM-00-总纲` §3、`ATOM-设计素材清单` §0-6。

## R8 · 内容服务与数据契约（可复用后端）

后端 cron 定期刷新内容池 → `GET /s1/content-pack?since&days=7&lang&themes&tier&region&type` 下发 → 前端预取 7 天 + 离线内置库兜底 + 14 天去重;Pro 门控在后端按授权过滤。
**对 App 的含义**：App 端人格/能量签内容**不必新建服务**，同一 content-pack API 可多端消费;App 需自带同样的离线兜底心态。
源：`ATOM-技术架构` §3-5。

## R9 · 关键参数速查（设备端已定）

| 参数 | 值 |
|---|---|
| 碎碎念间隔 / 停留 | 60s 随机 / 10s 自动消失 |
| doze 进入 | 10 分钟无操作 |
| 能量签 批量 / 上限 | 5 条/批 · 3 批/天 |
| 命中 / 保底 | 1/6·1/3·1/2 · 5 次保底 |
| 稀有度 | 普通70 / 稀有25 / SSR 5 |
| 内容去重 | 近 14 天 |

**对 App 的含义**：涉及共享机制（能量签/碎碎念）时沿用同值;App 独有节奏参数另行定义。
源：`ATOM-00-总纲` §9。

## R10 · 设备端待定项（App 讨论时避免踩进未拍板区）

语音技术栈（热词/ASR/TTS、端侧 vs 云、隐私）、节日清单最终版、主动 TTS 开关、Pro 定价/套餐与句库生产量级。若 App 设计依赖上述结论，标注依赖并挂入总纲决策清单。
源：`ATOM-00-总纲` §10、§12.3。
