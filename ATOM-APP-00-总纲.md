# ATOM App Home · 总纲（手机 App 首页 Home Tab 优化）

> 版本：v0.2　|　日期：2026-08-04　|　受众：产品 / 设计 / 研发
> 性质：**工作总纲**。目标、范围、基线方案与待讨论决策集中在此;随讨论逐步拍板、扩写、拆分专项文档。
> v0.2 变更：吸收 **v1 基线方案**（2026-06-08,见 `v1-基线方案/`）——补入现状问题盘点、基线信息架构,决策清单标记 v1 已定方向并并入其 6 个开放问题。

---

## 1. 背景与目标

BodyPark ATOM 手机 App 的首页 Home Tab 需要一轮优化设计。本项目要回答三个问题：

1. **首页替用户解决什么**：打开 App 第一屏应该承载什么任务。→ v1 已给出方向：**「今天该做什么」优先**（新用户=生成计划,老用户=今日训练）。
2. **与设备端 Companion Home 的关系**：App 首页在多大程度上呼应设备端的拟人化人格与机制（见 `ATOM-APP-REF-Companion呼应点.md`）。→ **v1 未涉及,本轮核心增量议题**（D2）。
3. **信息架构与优先级**：首屏各模块的取舍与排序原则。→ v1 已给基线（见 §3）,本轮在其上迭代。

### 1.1 现状问题盘点（v1 诊断,源:`v1-基线方案/首页改版方案-v1.md` §一）

| # | 问题 | 表现 |
|---|---|---|
| 1 | 空状态是"死区" | My Schedule 咖啡杯插画、"No Templates" 占近半屏且不可操作;CTA 与空状态视觉割裂 |
| 2 | 设备卡占位过大 | 连接正常时"已连接"只是状态确认,不值得占首屏约 1/4 |
| 3 | 模块缺失 | 无教程引导、无运营位、无数据回顾、无连续打卡 |
| 4 | 首页静态 | 新老用户看到完全相同的页面,而两类人群核心诉求差异很大 |

## 2. 范围

- **In**：手机 App 的 Home Tab —— 信息架构、模块定义、交互规格、视觉方向、与设备端的联动逻辑、落地的工程设计。
- **Out**：其他 Tab（动作库、历史、我的等）只在与首页有入口/联动关系时涉及;设备端首页本体（归 `ATOM-Companion-Home` 仓库）。

## 3. 基线方案 v1（2026-06-08,进一步讨论的起点）

> 完整内容见 [`v1-基线方案/首页改版方案-v1.md`](v1-基线方案/首页改版方案-v1.md),可交互原型 [`v1-基线方案/交互原型-v1.html`](v1-基线方案/交互原型-v1.html)（浏览器打开,可切新/老用户、点设备条切异常态）。

**v1 四原则**：① 空状态→行动卡;② 状态驱动(新/老用户 × 设备状态动态渲染);③ 优先级分层(今天该做什么>运营>内容分发);④ 常驻的是入口不是内容。

**v1 信息架构**：

```
顶部问候 + 铃铛(常驻活动/消息入口,红点)
├─ 设备状态条(正常压扁单行 / 异常展开警示大卡)
├─ 强时效公告条(仅重大事件,可关闭)
├─ 【动态区】新用户:生成计划英雄卡 + 新手任务清单(5步) + 训练答疑 Tips
│           老用户:今日训练卡(黑卡) + 周历打卡 + 本周数据条 + 推荐 Tips
├─ 官方活动轮播卡位(常驻展示位,三层运营结构之一)
├─ 创建自定义模板(空状态折叠为单行入口)
├─ 精选训练(横滑) └─ 精选计划
```

## 4. 设计原则（v1 四原则继承,新增草案待确认）

- **首屏一个主任务**（v1 已立:今天该做什么优先）。
- **状态驱动**（v1 已立:新/老 × 设备状态;可进一步细分维度,见 D8 休息日等）。
- **人格呼应而非复刻**：设备端表情卡是「脸」;App 首页是否/如何引入人格层,按场景取舍,不生搬圆屏交互。（🆕 本轮增量）
- **有设备与无设备用户都成立**：设备模块可插拔,不绑架无设备用户首屏。（🆕 待确认:v1 未覆盖无设备态）

## 5. 待讨论决策清单

> D1–D6 为本轮框架议题;D7–D12 并入自 v1 遗留开放问题（源:v1 方案 §五）。

| # | 议题 | 状态 | 结论 / 备注 |
|---|---|---|---|
| D1 | 首页的首要任务定位 | 🟡 v1 已定方向 | 「今天该做什么」优先,新/老用户二分;本轮确认是否维持 |
| D2 | 拟人化人格层在 App 端的形态（问候/碎碎念/能量签,呼应 R1/R4/R5） | ⏳ 待讨论 | v1 未涉及;本轮核心增量 |
| D3 | 连胜 Streak 与设备端共用信号源（R6:当日≥1组Set） | ⏳ 待讨论 | v1 今日卡已含「连续🔥12天」,口径未对齐 |
| D4 | 设备状态/联动模块 | 🟡 v1 已定基线 | 正常压扁/异常展开;**无设备用户态未覆盖**,待补 |
| D5 | 首屏模块清单与排序 | 🟡 v1 已给基线 | 见 §3;本轮在其上迭代 |
| D6 | 视觉语言与设备端的关系（R7） | ⏳ 待讨论 | v1 原型=白底+荧光绿(lime);设备端=暗底;两端如何「一家人」 |
| D7 | 新手任务②③(资料/偏好)放注册流程还是首页清单 | ⏳ v1 遗留 | 影响注册流失率,需评估 |
| D8 | 老用户「今日休息日」主卡样式 | ⏳ v1 遗留 | v1 建议恢复建议/拉伸课;可结合 D2 人格层表达 |
| D9 | 官方活动位无活动时的降级策略 | ⏳ v1 遗留 | 隐藏区块 vs 常青内容 |
| D10 | 周历打卡与 History 页数据口径对齐 | ⏳ v1 遗留 | 与 D3 同属「打卡口径」问题,建议合并讨论 |
| D11 | 铃铛=活动中心还是统一消息中心 | ⏳ v1 遗留 | 需与推送策略一起定;可参考 R3 仲裁分层 |
| D12 | 模板有内容时的卡片样式、与「生成计划」动线的关系 | ⏳ v1 遗留 | v1 未出样式 |

> 拍板后：结论写入本表并在对应专项文档展开;新增议题继续追加 D#。

## 6. 文档地图（规划）

| 文档 | 内容 | 状态 |
|---|---|---|
| 本文 `ATOM-APP-00-总纲.md` | 目标 / 范围 / 基线 / 原则 / 决策清单 | 🚧 v0.2 |
| `ATOM-APP-REF-Companion呼应点.md` | 设备端逻辑提炼参考 R1–R10 | ✅ |
| [`v1-基线方案/`](v1-基线方案/) | 历史阶段方案 v1（说明 + 可交互原型）,**只读存档,不再改写** | ✅ 基线 |
| `ATOM-APP-01-信息架构.md` | v2 首页模块清单、层级与状态矩阵 | 📝 待 D1/D5 复核后立稿 |
| `ATOM-APP-02-<模块>-PRD.md` | 各模块专项 PRD | 📝 按需 |
| `ATOM-APP-技术设计.md` | 数据契约 / 与设备端共享服务复用（R8） | 📝 待定 |
| `ATOM-APP-设计素材清单.md` | 视觉/动效素材交付清单 | 📝 待定 |
