# ATOM App Home · 首页立意:从内容货架到今日执行面

> 版本：v0.1（讨论稿）　|　日期：2026-08-04　|　受众：产品 / 设计 / 研发
> 性质：回应 2026-08-04 讨论提出的四个问题(社区/与我相关/打开即练/分发感),给出首页立意与结构提案。拍板后并入信息架构文档,决策项挂总纲 D13–D16。

---

## 1. 问题:v1 的隐性立意是「分发」

v1 解决了空状态和状态驱动,但通读其信息架构会发现:除「今日训练卡」外,首页大半屏幕(训练答疑/官方活动/自定义模板/精选训练/精选计划)是**把其他 Tab 和模块的内容搬上首页做分发,期待用户自己挑**。这隐含的立意是「首页=商场导购」。

问题在于:ATOM 有一台 **PLAN 引擎**(见 `ATOM-APP-REF-目标偏好呼应点.md` P1)——系统本来就知道用户的目标、偏好、现状,并据此开出了处方。**让拥有私人教练的用户自己逛货架,是产品能力的浪费**,也直接对冲「打开就可以开始运动」的主题。

## 2. 立意提案:首页 = PLAN 闭环的每日渲染面

一句话:**打开首页,像走进健身房时教练迎上来说的第一句话**——今天练这个、因为你要的是那个、你已经走到这里了。

对应数据闭环(P1):

| 教练的话 | 数据来源 | 首页职责 |
|---|---|---|
| 「今天练这个」 | PLAN(今日课) | 呈现 + 一键开始 |
| 「因为你要的是那个」 | GOAL(Intent 原话/milestone → Target) | 一句「为什么」(P2) |
| 「你已经走到这里了」 | LOG/STATUS vs Target | 一个主进度指标(P4/P5) |
| 「顺便,这些你可能感兴趣」 | 运营/内容(按 PREFERENCE 过滤,P6) | 降权到下屏 |

## 3. 三区模型(结构提案)

```
A 今日执行区(首屏,永不被挤占)
   人格问候(D2,Natter 驱动,素材=mindset/P3)
   今日主卡(状态驱动) + 一句「为什么练这个」
   主 CTA:开始训练 —— 拇指一次点击可达
B 与我相关区(首屏下缘~第二屏)
   主目标进度模块(P4 四种可视化按主目标选 1)
   周历/Streak(口径=R6/D10 统一信号源)
   训练后回流亮点(昨日质量评分/能量签 R5…)
C 探索与运营区(第二屏以下,可折叠)
   官方活动 · 精选内容(按 PREFERENCE 过滤) · 模板入口
   —— 未来社区 digest 卡位也落在这里(§5)
```

**三区契约**:
- **A 区排他**:任何运营/内容/社区模块不得进入 A 区或将其推出首屏(继承 v1 原则③并升级为硬约束;唯一例外=设备异常警示,呼应 v1 设备条规则)。
- **B 区只放「关于我的事实」**:数据必须与用户主目标关联(千卡这类通用指标让位于目标口径的进度,如身体构成进度条/进阶阶梯,P4)。
- **C 区是货架,承认它是货架**:允许存在,但降权、个性化过滤、可折叠;它的 KPI 是长尾发现,不是首屏转化。

## 4. 「打开就可以开始运动」:主 CTA 状态矩阵(草案)

零决策开练:系统已经替用户决定了,首页呈现**那一个决定**,而不是一组选择。

| 用户/系统状态 | A 区主卡 | 主 CTA | 次动线 |
|---|---|---|---|
| 有计划·今日有课 | 今日训练卡+为什么 | **开始训练** | 换一节/调整 |
| 有计划·今日休息日 | 恢复日卡(人格表达,D8) | **开始拉伸**(10min 恢复课) | 明日预告 |
| 无计划(老用户) | 生成计划英雄卡 | **立即生成** | ⚡快速开练 |
| 新用户未首训 | 英雄卡+新手任务 | **开始首训**(10min 体验课) | 浏览预置计划 |
| 设备断连/异常 | 警示卡(v1 规则) | **重新连接** | 无设备跟练(依赖 D4 无设备态) |
| 无设备用户(若服务,见 D4) | 今日跟练卡 | **开始跟练** | — |

- **⚡快速开练**(全局兜底,候选=底部 lime FAB 的语义):按 PREFERENCE 资源约束(时间/器械,P6)即时给一节课,始终一步可达——服务「今天没计划/没心情走完整流程但想动一下」的时刻。
- 衡量口径建议:**首屏开练转化率 + 打开→开始训练的中位时长**,替代泛泛的首页点击分布。

## 5. 未来上社区,首页怎么变(D13)

**骨架不变,社区不进 A 区**。社区内容以两种形态进入现有结构:

1. **社会证明作佐料,融进 A/B 区既有模块**:挑战卡的「3,204 人已参加」(v1 已有)、周历/Streak 的「好友连胜对比」、训练卡的「今天已有 x 人练了这节课」——增强动机,不新增模块。
2. **社区 digest 卡位进 C 区**:关注的人的动态摘要/热帖精选,一张横滑卡位,点击去社区 Tab。**信息流(feed)本体永远在社区 Tab,不上首页**——feed 是注意力流沙,与「打开就练」直接冲突。

即:社区上线 = C 区新增一个卡位 + A/B 区若干社会化字段,**不触发首页重构**。这正是三区契约的意义:未来任何新业务(商城/课程直播/社区)都先回答「你是 A、B 还是 C」。

## 6. 「与我相关」的三个机制(D14)

1. **用他自己的话**:问候/主卡副文案引用 `user_intent_raw` 与 milestone(P2)——「距离十月婚礼还有 9 周」比任何算法推荐都更「懂我」。
2. **解释每一天**:今日卡带一句由 Target 派生的「为什么」(「今天练推类,是你『改善圆肩』计划的第 2/3 周」)——把 PLAN 的可解释性变成信任感。依赖:goal 仓 `aggregate` 呈现口径(P8,跨仓议题)。
3. **一个主进度指标**:按最高 priority 目标从 P4 四种可视化中选一个上 B 区;目标切换,首页跟着换脸——首页外观本身成为「我的目标」的函数。

## 7. 新增决策项(已挂总纲)

| # | 议题 | 依赖 |
|---|---|---|
| D13 | 社区上线后的首页承接:三区契约(digest 进 C + 社会证明进 A/B,feed 不上首页)是否成立 | 社区产品定义 |
| D14 | 「为什么练这个」可解释文案:派生规则与口径 | goal 仓 aggregate 口径(P8) |
| D15 | 零决策开练:主 CTA 状态矩阵定稿 + ⚡快速开练动线(FAB 语义) | D4(无设备用户是否服务) |
| D16 | B 区主进度模块选型(P4 四选一 + 切换规则) | goal 仓 STATUS 细化(P8) |

## 8. 对 v1 的处置

v1 不推翻:A 区≈v1 动态主区、C 区≈v1 运营/内容区,B 区是本轮增量(v1 数据条的目标化升级)。v2 信息架构文档(`ATOM-APP-02-信息架构.md`,待立)将以三区模型为骨架重排 v1 模块清单。
